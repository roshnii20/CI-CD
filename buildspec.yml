version: 0.2

phases:
  install:
    runtime-versions:
        php: 7.3
    commands:
      - echo install
  pre_build:
    commands:
      - echo pre build
  build:
    commands:
      - echo build

  post_build:
    commands:
      - echo post build
artifacts:
  files:
      - '**/*'
